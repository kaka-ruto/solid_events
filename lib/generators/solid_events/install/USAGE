Add a `solid_events` database in `config/database.yml` and load `db/events_schema.rb`.
Prepare the events database from schema:

  bin/rails db:prepare

Mount the UI in routes:

  mount SolidEvents::Engine, at: "/solid_events"
